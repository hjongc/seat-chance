아래는 지금까지 결정된 내용을 반영한 **정리본 v0.2**입니다.  
기존 아이디어를 유지하되, **모바일 퍼스트 웹 서비스**, **프리티어 기반 운영**, **더미 API 없이 실제 데이터 기반 MVP** 방향으로 재구성했습니다.

---

# 앉을각 기획문서 v0.2

## 1. 서비스 개요

### 서비스명
**앉을각**

### 한 줄 정의
서울 지하철 이용자가 특정 구간을 이동할 때, **좌석 회전 가능성이 높은 호차·문 위치를 추천하는 모바일 퍼스트 웹 서비스**.

### 서비스 목적
출퇴근 지하철에서 사용자가 다음 질문에 바로 답을 얻도록 한다.

> 지금 내가 이 역에서 저 역까지 갈 때, 어느 호차·어느 문 근처에 서 있으면 좌석이 날 가능성이 높은가?

이 서비스는 “덜 붐비는 칸”을 찾는 앱이 아니다.  
핵심은 **중간역에서 내릴 사람이 많고, 그 하차·환승·출구 동선이 몰리는 문 위치를 찾는 것**이다.

---

## 2. 제품 방향

### MVP 방향
초기에는 앱스토어 앱이 아니라 **모바일 웹/PWA**로 시작한다.

이유:

- 데이터 모델과 추천 로직 검증이 먼저다.
- 앱 설치 없이 바로 테스트할 수 있다.
- 출퇴근 중 모바일 브라우저에서 빠르게 사용할 수 있다.
- 나중에 PWA 또는 Capacitor 기반 앱스토어 패키징이 가능하다.

### 확장 방향

1. 모바일 웹 MVP
2. PWA 설치 지원
3. 사용자 피드백 수집
4. Capacitor로 앱스토어/플레이스토어 패키징
5. 필요 시 React Native/Expo 전환 검토

처음부터 네이티브 앱으로 가지 않는다.  
초기 리스크는 UI가 아니라 **데이터 신뢰도와 추천 설명력**에 있다.

---

## 3. MVP 범위

### 초기 노선
서울 지하철 **3호선**

### 초기 구간
경복궁 → 신사 또는 경복궁 → 양재

### 초기 방향
오금 방면 / 대화 방면

### 초기 시간대
평일 출근 시간 07:00~10:00

### 초기 출력
호차-문 Top 3 추천

예시:

```text
경복궁 → 신사
3호선 오금 방면
평일 08:30 기준

1위 2-3 문 | 좌석 회전 점수 82점
2위 3-2 문 | 좌석 회전 점수 74점
3위 4-1 문 | 좌석 회전 점수 66점
```

---

## 4. 핵심 가설

좌석이 날 가능성은 단순 혼잡도보다 다음 요인에 더 크게 영향을 받는다.

```text
1. 중간역에서 내릴 사람이 많은가
2. 그 역이 환승역인가
3. 환승 동선과 가까운 문인가
4. 출구·계단·엘리베이터 동선과 가까운 문인가
5. 그 역에서 새로 타는 사람이 너무 많지는 않은가
6. 전체 혼잡도가 좌석 회전 가능성을 압도할 정도로 높은가
7. 좌석이 나더라도 도착역이 너무 가까운 것은 아닌가
```

---

## 5. 풀지 않는 문제

MVP에서는 다음을 하지 않는다.

```text
실시간 좌석 수 예측
개별 승객 목적지 예측
CCTV 기반 혼잡도 분석
Bluetooth/Wi-Fi 기반 위치 추적
LLM 기반 추천
머신러닝 기반 착석 확률 모델
```

추천 결과는 확률이 아니라 **동일 경로 내 상대 점수**다.

금지 표현:

```text
2-3 문에 서면 앉을 확률 82%
```

권장 표현:

```text
2-3 문 좌석 회전 점수 82점
이 구간 내 다른 문 대비 좌석 회전 가능성이 높습니다.
```

---

## 6. 서비스 구조

### 결정된 방향
더미 API 없이, 처음부터 실제 운영 구조로 간다.

### 권장 아키텍처

```text
Frontend/API: Next.js
Database: Neon Postgres 또는 Supabase Postgres
Batch: GitHub Actions scheduled workflow
Hosting: Vercel Hobby
App 확장: PWA → Capacitor
```

초기에는 FastAPI를 별도 서버로 띄우지 않는다.  
프리티어 운영에서는 서버 상시 구동 비용과 콜드스타트 리스크가 있으므로, Next.js API Route 또는 Route Handler에서 추천 API를 제공한다.

---

## 7. 프리티어 클라우드 전략

### 1순위 조합

| 영역 | 서비스 | 이유 |
|---|---|---|
| 웹/API | Vercel Hobby | Next.js 배포에 적합, 무료 시작 가능 |
| DB | Neon Free Postgres | Postgres 기반, 서버리스 성격, 무료 시작 가능 |
| 대안 DB | Supabase Free | Postgres + 관리 UI + Auth 확장 가능 |
| 배치 | GitHub Actions | 정기 데이터 수집/정제 가능 |
| 앱 확장 | Capacitor | 웹앱을 앱스토어용 패키지로 확장 가능 |

참고:
- [Vercel Hobby](https://vercel.com/docs/plans/hobby)
- [Neon Pricing](https://neon.com/pricing)
- [Supabase Billing](https://supabase.com/docs/guides/platform/billing-on-supabase)
- [GitHub Actions Billing](https://docs.github.com/en/billing/managing-billing-for-github-actions/about-billing-for-github-actions)

### 보류할 조합

Render/Railway에 백엔드 서버를 상시 구동하는 방식은 초기에는 보류한다.  
무료 계층이 운영용으로 안정적이지 않거나, 제한이 작기 때문이다.

참고:
- [Render Free](https://render.com/docs/free)
- [Railway Free Trial](https://docs.railway.com/pricing/free-trial)

---

## 8. 데이터 전략

### 주요 공공데이터

1. 역별·시간대별 승하차 인원  
   역별 하차량, 승차량, 시간대별 패턴 산출에 사용

2. 지하철 혼잡도 정보  
   혼잡도 페널티와 사용자 안내에 사용

3. 환승정보  
   환승역에서 어느 호차·문이 환승 동선과 가까운지 계산

4. 빠른하차정보  
   계단, 엘리베이터 등 이동설비와 가까운 문 위치를 계산

5. 열차운행현황  
   노선별 편성당 칸수 확인에 사용

참고:
- [서울교통공사 서울 도시철도 환승정보](https://www.data.go.kr/data/15098252/fileData.do)
- [서울교통공사 빠른하차정보](https://www.data.go.kr/data/15143840/openapi.do)
- [서울교통공사 열차운행현황](https://www.data.go.kr/data/3052776/fileData.do)
- [서울교통공사 열차시간표](https://www.data.go.kr/data/15143847/openapi.do)

---

## 9. 열차 위치 UI 설계 원칙

열차는 노선마다 량 수와 문 개수가 다를 수 있다.  
따라서 UI에서 `10량 x 4문`을 하드코딩하지 않는다.

### train_layout 테이블

```sql
train_layout (
  operator,
  line_no,
  branch_code,
  direction_code,
  car_count,
  doors_per_car,
  source,
  confidence,
  valid_from,
  valid_to
)
```

### API 예시

```http
GET /api/v1/train-layout?line_no=3&direction=오금
```

### 응답 예시

```json
{
  "line_no": "3",
  "direction": "오금",
  "car_count": 10,
  "doors_per_car": 4,
  "source": "서울교통공사_열차운행현황 + 수동검증",
  "confidence": 0.9
}
```

프론트엔드는 이 값을 기준으로 문 위치 UI를 동적으로 렌더링한다.

---

## 10. 추천 모델

### 기본 공식

```text
SeatScore(D) =
중간역 하차 기회 점수
+ 환승문 가중치
+ 빠른하차/출구 동선 가중치
- 승차 혼잡 페널티
- 전체 혼잡도 페널티
- 도착 임박 페널티
```

### 점수 요소

| 요소 | 설명 |
|---|---|
| 하차량 | 중간역에서 내리는 사람이 많을수록 가점 |
| 환승문 | 환승 동선과 가까운 문일수록 가점 |
| 빠른하차/출구 | 계단·엘리베이터·출구 동선과 가까울수록 가점 |
| 승차량 | 같은 역에서 새로 많이 타면 감점 |
| 혼잡도 | 혼잡도가 높으면 좌석 경쟁이 커지므로 감점 |
| 거리 | 도착역 직전에서 생기는 좌석 기회는 낮게 반영 |

---

## 11. 추천 API

### 요청

```http
GET /api/v1/recommendations
```

### Query Parameters

```text
origin=경복궁
destination=신사
line_no=3
direction=오금
datetime=2026-05-07T08:30:00+09:00
mode=seat
```

### 응답 예시

```json
{
  "origin": "경복궁",
  "destination": "신사",
  "line_no": "3",
  "direction": "오금",
  "time_slot": "08:00",
  "recommendations": [
    {
      "rank": 1,
      "car_no": 2,
      "door_no": 3,
      "score": 82.4,
      "grade": "HIGH",
      "expected_seat_window": "종로3가~충무로",
      "reasons": [
        "종로3가 하차/환승 수요가 높습니다.",
        "충무로 환승 동선과도 가까운 위치입니다.",
        "목적지보다 충분히 앞선 구간에서 좌석 회전 가능성이 있습니다."
      ]
    }
  ],
  "cautions": [
    "점수는 실제 착석 확률이 아니라 동일 경로 내 상대 추천 점수입니다.",
    "실제 열차 혼잡, 지연, 행사, 날씨 등은 반영되지 않을 수 있습니다."
  ]
}
```

---

## 12. 모바일 화면 구성

### 홈 화면

```text
출발역
도착역
출발 시간
방향
[추천 보기]
```

MVP에서는 `앉을 가능성 우선`만 제공한다.

### 결과 화면

```text
경복궁 → 신사
3호선 오금 방면
평일 08:30 기준

앉을 가능성이 높은 위치

1위 2-3 문
좌석 회전 점수 82점
예상 기회 구간: 종로3가~충무로

근거:
- 종로3가 하차/환승 수요 높음
- 충무로 환승 수요 반영
- 목적지보다 앞선 구간에서 좌석 회전 가능
```

### 열차 위치 UI

`train_layout` 기반으로 동적 렌더링한다.

```text
1호차      2호차      3호차
[1-1]     [2-1]     [3-1]
[1-2]     [2-2]     [3-2]
[1-3]     [2-3 BEST][3-3]
[1-4]     [2-4]     [3-4]
```

---

## 13. 배치 파이프라인

### 수집

```text
역별 시간대별 승하차 인원
혼잡도 정보
환승정보
빠른하차정보
열차운행현황
수동 보정 테이블
```

### 정제

```text
역명 표준화
호선 번호 표준화
방향 코드 표준화
시간대 단위 통일
요일 구분 생성
결측치 처리
```

### 생성

```text
ridership_profile
congestion_profile
transfer_door
exit_or_facility_door
train_layout
recommendation_cache
```

---

## 14. MVP 개발 순서

1. Next.js 프로젝트 생성
2. Postgres 연결
3. 핵심 스키마 작성
4. 3호선 역 순서 데이터 적재
5. 열차 레이아웃 데이터 적재
6. 승하차/환승/빠른하차 데이터 수집 배치 작성
7. 추천 점수 계산 API 구현
8. 모바일 결과 화면 구현
9. 열차 위치 UI 동적 렌더링
10. 경복궁 → 신사 구간으로 현장 검증

---

## 15. 최종 MVP 정의

```text
서울 지하철 3호선 일부 구간에서
역별 시간대별 승하차량, 환승 하차문, 빠른하차 위치, 혼잡도 데이터를 이용해
좌석 회전 가능성이 높은 호차-문 위치 Top 3를
모바일 웹에서 추천하는 서비스
```

MVP의 핵심은 AI가 아니다.

```text
정확한 역 순서
신뢰 가능한 공공데이터 적재
하차/환승/출구 동선 기반 문별 가중치
도착 임박 페널티
승차량과 혼잡도 감점
확률이 아닌 상대 점수 표현
```

초기에는 작게 시작하되, 구조는 나중에 전체 수도권과 앱스토어 확장까지 갈 수 있도록 잡는다.